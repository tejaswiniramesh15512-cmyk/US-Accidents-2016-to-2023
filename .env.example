# Copy this file to .env before running docker-compose up.
# These are local development defaults only — before any real deployment,
# replace them and move secrets to Docker Secrets / a secrets manager
# (see Section 7 of the Conception Phase document).

MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

POSTGRES_USER=accidents_app
POSTGRES_PASSWORD=changeme
POSTGRES_DB=accidents
